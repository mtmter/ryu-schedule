# AGENTS.md

## Project Overview

このリポジトリは、「はじめてのハッカソン 2026 Summer」に向けて開発しているWebアプリです。

テーマは「よりよいスケジュール帳」です。

予定とタスクをまとめて扱えるスケジュール帳を開発しています。

開発メンバーは4人で、全員Webアプリ開発の初心者です。

そのため、このプロジェクトでは高度な設計や技術的な巧妙さよりも、以下を優先してください。

- 初心者が読んで理解しやすいこと
- 小さな変更を積み重ねること
- 実際に動くこと
- 既存機能を壊さないこと
- チームメンバーが後から修正しやすいこと
- 不要な技術やライブラリを増やさないこと

---

## Tech Stack

基本構成は以下です。

### Frontend

- React
- Vite
- JavaScript
- CSS

### Backend

- Python
- FastAPI
- Uvicorn

### Database

- SQLite

特別な理由がない限り、この技術構成を変更しないでください。

以下のような新しい技術を勝手に導入しないでください。

- TypeScript
- Docker
- ORM
- Reduxなどの大規模な状態管理ライブラリ
- UIフレームワーク
- 新しいデータベース

導入が必要だと考えた場合は、実装するのではなく理由を報告してください。

---

## Source of Truth

実装内容は原則としてGitHub Issue単位で管理します。

タスクを依頼された場合は、作業開始前に必ず指定されたIssueを確認してください。

情報の役割は以下のように考えてください。

- `AGENTS.md`: プロジェクト全体で常に守るルール
- GitHub Issue: 今回実装する具体的な機能・仕様
- 現在のコード: 実際に動いている既存仕様
- README: 開発環境や起動方法

具体的な機能仕様については、AGENTS.mdではなくIssueを優先してください。

ただし、Issueと現在の実装が食い違っている場合は、周辺コードを確認してから変更してください。

矛盾を解決するために大規模な変更が必要になる場合は、勝手に変更せず問題点を報告してください。

---

## Working with GitHub Issues

原則として、1回の作業では指定されたIssueの範囲だけを実装してください。

### 作業開始前

以下を行ってください。

1. 指定されたIssueを読む
2. 関係する既存コードを読む
3. 既存のAPI・DB・コンポーネントへの影響を確認する
4. 変更予定のファイルと実装方針を簡潔に整理する
5. その後に実装を開始する

### Scope

以下を守ってください。

- Issueに書かれていない機能を勝手に追加しない
- 他のIssueで担当されている機能を先回りして実装しない
- 必要以上のリファクタリングをしない
- 関係のないファイルを変更しない
- UIを勝手に大幅変更しない
- APIやDB仕様を必要以上に拡張しない

将来的に必要そうな機能を見つけても、今回のIssueに不要なら実装せず、作業終了時に提案として報告してください。

---

## Existing Code First

変更前に必ず現在のコードを確認してください。

「新しく作る」よりも「既存コードを利用して最小限変更する」ことを優先してください。

特に変更内容に応じて以下を確認してください。

### Frontend

- `frontend/package.json`
- `frontend/src/`
- 関係するReactコンポーネント
- APIを呼び出している既存コード
- CSS

### Backend

- `backend/main.py`
- `backend/database.py` が存在する場合はその内容
- `backend/requirements.txt`
- 既存のAPIエンドポイント
- SQLiteのテーブル作成処理

### Project

- `.gitignore`
- `README.md`
- `AGENTS.md`

ファイル構成は今後変わる可能性があるため、存在を決めつけず実際のリポジトリを確認してください。

---

## Application Domain

このアプリでは主に以下のデータを扱います。

- 予定（events）
- タスク（tasks）

ただし、具体的なフィールド、APIエンドポイント、リクエスト・レスポンス形式については、指定されたIssueと現在のコードを確認してください。

AGENTS.mdに書かれていないフィールドや仕様を勝手に追加しないでください。

---

## Backend Guidelines

バックエンドでは、初心者でも処理を追える単純な構成を優先してください。

### 基本方針

- FastAPIを使用する
- SQLiteを使用する
- SQLやDB処理を必要以上に抽象化しない
- 不要なクラスを作らない
- 不要な設計パターンを導入しない
- ORMを勝手に導入しない
- 既存APIとの互換性をできるだけ保つ

APIは原則として `/api/` 以下に配置してください。

既存の `/api/health` は、特別な理由がない限り壊さないでください。

### API変更時

APIを追加・変更する場合は、以下を確認してください。

- HTTPメソッド
- URL
- リクエストデータ
- レスポンスデータ
- 存在しないIDなどのエラーケース
- フロントエンド側への影響

Issueに指定されていないAPI仕様を勝手に増やさないでください。

### Database変更時

テーブルやカラムを変更する場合は、以下を確認してください。

- 現在のテーブル定義
- 既存データへの影響
- APIへの影響
- フロントエンドへの影響

Issueの実装に不要なテーブルやカラムを追加しないでください。

ハッカソン用の小規模アプリなので、必要以上に複雑なマイグレーションシステムを導入しないでください。

---

## Frontend Guidelines

フロントエンドでは、Reactの基本的な機能で理解できるコードを優先してください。

### 基本方針

- 関数コンポーネントを使用する
- Reactの標準的なHooksを使用する
- 状態管理を必要以上に複雑にしない
- コンポーネントを細かく分割しすぎない
- API通信を必要以上に抽象化しない
- 外部ライブラリを安易に追加しない

コンポーネント分割は、実際に読みやすくなる場合に行ってください。

数行しか使わない処理のために新しいファイルを大量に作ることは避けてください。

### UI

現段階では、見た目の完成度より機能が正しく動くことを優先します。

ただし以下は守ってください。

- 入力欄の用途が分かる
- ボタンの用途が分かる
- データが読みやすく表示される
- エラーが起きた場合に完全に無反応にならない

Issueでデザインが指定されている場合は、その仕様に従ってください。

---

## Frontend / Backend Integration

フロントエンドとバックエンドの接続部分を変更するときは、片方だけを見て判断しないでください。

例えばAPIを変更する場合は、

1. FastAPI側の実装
2. React側のAPI呼び出し
3. リクエスト形式
4. レスポンス形式

を確認してください。

既存フロントエンドが利用しているAPIを変更する場合は、既存機能が壊れないか確認してください。

Issueの担当範囲を超える修正が必要になった場合は、必要最小限の互換対応にとどめるか、問題として報告してください。

---

## Coding Guidelines

このコードは初心者チームが読み、ハッカソン期間中に自分たちで修正します。

そのため、読みやすさを最優先してください。

### Prefer

- 意味の分かる変数名
- 意味の分かる関数名
- 短く単純な処理
- 一般的なReact / Pythonの書き方
- 明示的な処理
- 小さな変更

### Avoid

- 過度な抽象化
- 不要なクラス
- 不要な共通化
- メタプログラミング
- 複雑なデザインパターン
- 1つの関数に大量の処理を書くこと
- 理由のない大規模リファクタリング
- 「将来使うかもしれない」だけのコード

コードを短くすることより、読めば何をしているか分かることを優先してください。

### Comments

コードをそのまま読み上げるだけのコメントは不要です。

コメントを書く場合は、「何をしているか」よりも「なぜそうしているか」を説明してください。

---

## Dependencies

新しいライブラリを追加する前に、本当に必要か確認してください。

既存の依存関係や標準機能で簡単に実装できる場合は、新しいライブラリを追加しないでください。

新しい依存関係を追加した場合は、作業終了時に以下を報告してください。

- 追加したライブラリ
- 追加した理由
- どこで使用しているか

---

## Error Handling

ハッカソン用の小規模アプリなので、過度に複雑なエラー処理は不要です。

ただし、最低限以下は考慮してください。

- 不正な入力
- 存在しないデータ
- API通信失敗
- DB処理失敗

エラーを無視したり、例外を理由なく握りつぶしたりしないでください。

---

## Environment Variables and Secrets

このGitHubリポジトリはpublicで運用します。

以下の情報をコードやGitの履歴に含めないでください。

- APIキー
- アクセストークン
- パスワード
- 秘密鍵
- その他の秘密情報

秘密情報が必要になった場合は環境変数を使用してください。

`.env` など秘密情報を含むファイルはGit管理対象にしないでください。

既存の `.gitignore` も確認してください。

---

## Git Safety

明示的に依頼されていない限り、以下を勝手に行わないでください。

- `main` ブランチへの直接push
- merge
- rebase
- force push
- 既存ブランチの削除
- Git履歴の書き換え
- 関係のないファイルの削除

既存の変更が作業ディレクトリにある場合、それを勝手に削除・上書きしないでください。

---

## Verification

実装しただけで終了せず、可能な範囲で動作確認してください。

変更した範囲に応じて必要な確認を行ってください。

### Backend

最低限、以下を確認してください。

- Pythonコードに明らかなエラーがない
- FastAPIが起動できる
- 変更したAPIが期待どおり応答する
- 既存の関連APIが壊れていない

### Frontend

最低限、以下を確認してください。

- ビルドまたは開発サーバーが起動できる
- JavaScriptの明らかなエラーがない
- 変更した画面・操作が動く
- 関係するAPIと通信できる

既存のlintやテストコマンドが定義されている場合は、それらも利用してください。

実行できなかった確認については、実行したふりをせず理由を報告してください。

---

## Documentation

READMEなどのドキュメントは、今回の変更によって既存の説明が古くなる場合に更新してください。

単なる機能追加のたびにREADMEを必要以上に書き換える必要はありません。

起動方法、必要な依存関係、重要な開発手順などが変わった場合は更新してください。

---

## Definition of Done

Issueの作業は、原則として以下を満たしたとき完了と考えてください。

1. Issueで指定された機能が実装されている
2. Issue外の不要な機能を追加していない
3. 既存の関連機能をできるだけ壊していない
4. 変更したコードを可能な範囲で動作確認している
5. 秘密情報を追加していない
6. 作業内容を説明できる状態になっている

---

## Final Report

作業終了時には、簡潔に以下を報告してください。

1. 実装した内容
2. 変更したファイル
3. 実行したコマンド
4. 動作確認結果
5. 確認できなかったこと
6. Issue外で気づいた問題があればその内容

「次に実装するとよい機能」は、指定されたIssueの範囲外であればコードには追加せず、提案として報告してください。

---

## Most Important Rules

迷った場合は、以下の順番で判断してください。

1. 指定されたIssueを読む
2. 現在のコードを読む
3. 最小限の変更でIssueを満たす
4. 既存機能を壊さない
5. 初心者が理解できる実装にする
6. 分からないことを推測して大規模に作り込まない

このプロジェクトでは、複雑で完璧な設計よりも、チーム全員が理解しながら短期間で動くものを完成させることを優先してください。
